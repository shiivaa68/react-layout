import styled from 'styled-components';

const HeaderDetail = styled.section`
  width: fit-content;
  background-color: green;
  display: flex;
  justify-content: space-between;
  align-items: center;

  & button {
    margin: 0 0.5rem;
  }
`;

export default HeaderDetail;

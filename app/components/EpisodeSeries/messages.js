import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.components.EpisodeSeries';

export default defineMessages({
  episode: {
    id: `${scope}.episode`,
    defaultMessage: 'قسمت ',
  },
});
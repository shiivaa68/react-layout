import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.components.ScreenShots';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'تریلر، تصاویر و جزییات',
  },
});
import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.PackageDetails';

export default defineMessages({
  awards: {
    id: `${scope}.awards`,
    defaultMessage: 'جوایز و افتخارات',
  },
});
import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.component.SideBar';

export default defineMessages({
  logout: {
    id: `${scope}.logout`,
    defaultMessage: 'خروج',
  }
});
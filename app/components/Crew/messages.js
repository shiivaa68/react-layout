import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.components.Casts';

export default defineMessages({
  movies: {
    id: `${scope}.movies`,
    defaultMessage: 'فیلم ',
  },
  series: {
    id: `${scope}.series`,
    defaultMessage: 'سریال ',
  },
});
import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.components.EpisodeControl';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'فصل',
  },
});
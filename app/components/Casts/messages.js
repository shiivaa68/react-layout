import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.components.Casts';

export default defineMessages({
  subtitle: {
    id: `${scope}.subtitle`,
    defaultMessage: 'عوامل ',
  },
});
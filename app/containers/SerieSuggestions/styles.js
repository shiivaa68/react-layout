import styled, { css } from 'styled-components';


export const SuggestionSeriesLayout = styled.article`
  min-height: 100vh;
  display: flex;
  justify-content: flex-start;
  align-items: flex-start;
  flex-direction: column;
  color: var(--primary-light);

`;

export const Title = styled.h2`
 
  width: 100%;
  padding: 3rem;
`;

export const SuggestionSeriesWrapper = styled.section`
  position: relative;
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  padding: 0 2rem;
`;


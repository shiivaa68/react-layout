import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.components.ResultControl';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'فصل',
  },
});
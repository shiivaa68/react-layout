import styled from 'styled-components';

export const RegisterWrapper = styled.article`
  width: 100%;
  height: 100vh;
  background-color: 'red';
  display: flex;
  justify-content: center;
  align-items: center;
  color: var(--primary-light);
`;

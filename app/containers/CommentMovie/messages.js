import { defineMessages } from 'react-intl';
import { rules } from 'eslint-config-prettier';

export const scope = 'boilerplate.containers.CommentMovie';

export default defineMessages({
  commentMovie: {
    id: `${scope}.commentMovie`,
    defaultMessage: ' نظرات کاربران',
  },
});
